<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <AssemblyName>Bansa</AssemblyName>
    <RootNamespace>Bansa</RootNamespace>
    <Version>1.2.0</Version>
    <ApplicationIcon>Resources\Icon_bansa-dark.ico</ApplicationIcon>
    <Company>Personal</Company>
    <Product>Bansa</Product>
    <Description>Non-intrusive, reversible per-app bandwidth monitor and control for Windows.</Description>
  </PropertyGroup>

  <!--
    ══ Publish ════════════════════════════════════════════════════════════════════
    Run pack-release.ps1 to produce release\Bansa.zip.
    User data lives in %LocalAppData%\Bansa\; tool executables live in Data\Tools\
    next to Bansa.exe.
    ══════════════════════════════════════════════════════════════════════════════
  -->

  <ItemGroup>
    <Resource Include="Resources\Bansa.ico" />
    <Resource Include="Resources\Icon_bansa-dark.ico" />
    <Resource Include="Resources\bansa-light.png" />
    <Resource Include="Resources\bansa-dark.png" />
    <Resource Include="Resources\logo_openrgb.png" />
    <Resource Include="Resources\logo_hwinfo.png" />
    <Resource Include="Resources\logo_sharex.png" />
    <Resource Include="Resources\logo_ctt.png" />
    <Resource Include="Resources\logo_ddu.png" />
    <Resource Include="Resources\logo_fancontrol.png" />
    <Resource Include="Resources\logo_spacesniffer.png" />
    <Resource Include="Resources\logo_forcebindip.png" />
    <!-- Inter — bundled as WPF resources so they render identically
         on any Windows install regardless of whether the user has the font installed.
         Reference in XAML: pack://application:,,,/Fonts/#Inter -->
    <Resource Include="Fonts\Inter-Regular.ttf" />
    <Resource Include="Fonts\Inter-Medium.ttf" />
    <Resource Include="Fonts\Inter-SemiBold.ttf" />
    <Resource Include="Fonts\Inter-Bold.ttf" />
    <Resource Include="Fonts\Inter-Light.ttf" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Bansa.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.3.2" />
    <PackageReference Include="LibreHardwareMonitorLib" Version="0.9.4" />
    <PackageReference Include="Microsoft.Data.Sqlite" Version="8.0.10" />
    <PackageReference Include="Microsoft.Diagnostics.Tracing.TraceEvent" Version="3.1.16" />
    <PackageReference Include="System.Drawing.Common" Version="8.0.10" />
    <PackageReference Include="System.Management" Version="9.0.0" />
    <!-- System.Management is used by HardwareMonitor to query Win32_PhysicalMemory.Speed
         (RAM clock via SMBIOS/SPD) — not exposed as a LibreHardwareMonitor sensor.
         It is also pulled in transitively by LibreHardwareMonitorLib. -->
  </ItemGroup>

  <!--
    ══ Defender false-positive mitigation ════════════════════════════════════════
    Two sources of Defender false-positives are cleaned up here:

    1. KernelTraceControl.dll + msdia140.dll  — shipped by the TraceEvent NuGet
       package for heap-snapshot profiling (a feature Bansa never uses). Their
       presence alongside a kernel ETW session triggers the "kernel-level network
       sniffer" heuristic.  TraceEvent works fine without them.

    2. Bansa.sys — LibreHardwareMonitor 0.9.x extracts a WinRing0-based kernel
       driver at runtime, naming it "<ProcessName>.sys" so multiple monitoring
       tools can coexist.  Because it is unsigned, Defender flags it as a severe
       threat.  LHM's Computer.Close() deletes it on normal exit; this target
       cleans up any copy left behind in the build / publish tree from developer
       test runs.  The HardwareMonitor.Dispose() method also deletes it at
       runtime as a safety net when the app closes.

    These targets simply delete the files; they do NOT affect any functional
    code paths — ETW network sessions and hardware sensor polling continue to
    work correctly without them.
    ══════════════════════════════════════════════════════════════════════════════
  -->
  <Target Name="RemoveDefenderFlaggedDlls" AfterTargets="Build">
    <Delete Files="$(OutputPath)amd64\KernelTraceControl.dll" ContinueOnError="true" />
    <Delete Files="$(OutputPath)amd64\msdia140.dll"           ContinueOnError="true" />
    <Delete Files="$(OutputPath)Bansa.sys"                    ContinueOnError="true" />
  </Target>
  <Target Name="RemoveDefenderFlaggedDllsPublish" AfterTargets="Publish">
    <Delete Files="$(PublishDir)amd64\KernelTraceControl.dll" ContinueOnError="true" />
    <Delete Files="$(PublishDir)amd64\msdia140.dll"           ContinueOnError="true" />
    <Delete Files="$(PublishDir)Bansa.sys"                    ContinueOnError="true" />
  </Target>

</Project>
